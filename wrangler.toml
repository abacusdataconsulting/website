# Cloudflare Pages Configuration
# This file configures the Cloudflare Pages project

name = "abacus-site"
compatibility_date = "2024-01-01"

# Pages-specific settings
pages_build_output_dir = "./"

# Environment variables (set these in Cloudflare Dashboard for production)
# [vars]
# CONTACT_EMAIL = "abacus.data.consulting@gmail.com"

# Email Routing - send_email binding (locked to destination address)
[[send_email]]
name = "EMAIL"
destination_address = "abacus.data.consulting@gmail.com"

# For local development with wrangler pages dev
[dev]
port = 8788
